import React, {useState, useEffect} from 'react';

const UserManageSubscriptions = (props) => {

  return (
    <div className="user-profile-subscriptions">
      // Manage Subscriptions
    </div>
  );
};

export default UserManageSubscriptions;