import React, {useState, useEffect} from 'react';

const UserAccountSettings = (props) => {

  return (
    <div className="user-profile-account-settings">
      // Account Settings
    </div>
  );
};

export default UserAccountSettings;